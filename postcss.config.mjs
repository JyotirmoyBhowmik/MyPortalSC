/**
 * PostCSS Configuration — Integrates TailwindCSS v4.
 * Processes styles loaded in globals.css and applies utility classes.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
