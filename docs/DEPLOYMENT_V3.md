# Deployment V3 Guide — Comprehensive Content Update

This guide outlines the specifications for V3 of the Jyotirmoy Bhowmik Portfolio, detailing the massive content expansion, "Deep Modern" aesthetic refinements, and database population for 88+ initiatives.

## 1. Version Highlights

- **Aesthetic**: Deep Modern (Navy/Slate/Electric Green).
- **Core Content**: Updated Professional Summary, Vision Statement, and Personal Details.
- **Projects**: Expanded from 5 placeholders to **88+ real-world initiatives** spanning FY 2024–2017.
- **Experience**: Integrated detailed "Professional Experience" timeline into the About page.
- **Skills**: Refined to match "Core Competencies" (IT Infrastructure, OT Security, Cloud).

## 2. Updated Database Schema & Seed Data

The `projects` table remains the core, but the seed data (`004_seed_data.sql`) has been completely rewritten to include:
- 15+ Core Competencies/Skills.
- 3 Explicit Professional Experience entries (ITC Infotech, Payoda, Global Innov Source).
- ~88 Projects/Initiatives categorized by domain (OT Security, Network Infra, Cloud Strategy, etc.).

### Key Data Structures

**Projects Table**:
- `title`: Global Standard Project Title.
- `slug`: Unique identifier (e.g., `industrial-ot-network-segmentation`).
- `detailed_description`: Merged Strategy, Criticality, and Delivery Focus.
- `domain`: Tags like `{"OT Security", "Critical"}`.

**Content Pages (JSONB)**:
- Now includes an `experience` array field in the `about` page content for the timeline view.

## 3. Deployment Instructions

### Step 1: Push Code Changes
Push the latest code (including the updated `AboutPage` component) to GitHub.

```bash
git add .
git commit -m "feat: portfolio v3 content overhaul"
git push origin main
```

### Step 2: Run Database Migrations (Critical)
The most important step for V3 is populating the database.

1.  Log in to **Supabase Dashboard**.
2.  Open the **SQL Editor**.
3.  Open/Copy the content of **[004_seed_data.sql](../supabase/migrations/004_seed_data.sql)**.
4.  Run the script.
    *   *Note: This script uses `ON CONFLICT` to update existing records, but for a clean slate, you might consider truncating the `projects` table first if you have old dummy data you want to remove.*

### Step 3: Verification
1.  **About Page**: Check the new "Vision Statement" and "Professional Experience" timeline.
2.  **Projects Page**: Verify that dozens of projects are listed, showing the depth of experience.
3.  **Project Details**: Click into a project (e.g., "Industrial OT Network Segmentation") to see the detailed description and tags.

## 4. Environment Variables
No new environment variables are required for V3. Ensure the standard Supabase keys are set in Vercel.

---
*Generated by Antigravity Agent - 2026*
